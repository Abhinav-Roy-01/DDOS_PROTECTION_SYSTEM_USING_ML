# Copy to .env and fill in real values. Never commit the real .env file.
# PHASE: 3 (hardening) -- SERVER.py, CAPTCHA_ROUTES.py, and AUTH.py should
# read all of these from environment, with NO hardcoded fallback values.

ADMIN_API_KEY=
CAPTCHA_SECRET_KEY=
PARTIAL_TOKEN_SECRET=
FULL_TOKEN_SECRET=
REDIS_URL=redis://localhost:6379/0
FLASK_ENV=production
