global:
  scrape_interval: 15s
scrape_configs:
  - job_name: dns-resolver
    static_configs: [{ targets: ['dns-resolver:9100'] }]
  - job_name: api
    static_configs: [{ targets: ['api:8000'] }]
  - job_name: http-layer-shield
    static_configs: [{ targets: ['http-layer-shield:8080'] }]
